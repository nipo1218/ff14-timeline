rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAdmin() {
      return request.auth != null && request.auth.token.email == '0miyanipo@gmail.com';
    }
    
    match /global/{document} {
      allow read: if true;
      allow write: if isAdmin();
    }
  }
}